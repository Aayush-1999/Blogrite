<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Blogrite</title>
        <!--Import Google Icon Font-->
        <link rel="icon" href="/images/icons/Artboard-512.png">
        <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
        
        <!-- Font Awesome fonts -->
        <link rel="stylesheet" href="/stylesheets/font-awesome.min.css"/>
        <link type="text/css" rel="stylesheet" href="/stylesheets/style.css"  media="screen,projection"/>
        <!--Import animate.css-->
        <link type="text/css" rel="stylesheet" href="/animate.min.css" />
        <!--Import materialize.css-->
        <link type="text/css" rel="stylesheet" href="/css/materialize.min.css"  media="screen,projection"/>
        <!-- Import classic ckeditor -->
        <script src="/ckeditor.js"></script>
        <!--Add link rel manifest -->
        <link rel="manifest" href="/manifest.json">

        <!-- Add iOS meta tags and icons -->
        <meta name="apple-mobile-web-app-capable" content="yes">
        <meta name="apple-mobile-web-app-status-bar-style" content="black">
        <meta name="apple-mobile-web-app-title" content="Blogrite">
        <link rel="apple-touch-icon" href="/images/icons/Artboard-152.png">
        
        <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>  
        
        <!--Add description here -->
        <meta name="description" content="A blogging web app">

        <!-- Add meta theme-color -->
        <meta name="theme-color" content="#2F3BA2" />
        
        <!--Let browser know website is optimized for mobile-->
        <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    </head>
    <body>
            
<% include ./navbar.ejs %>

        <div class="container">
            <% if(error && error.length>0) { %>
                <div class="msg msg-error z-depth-3 scale-transition">
                    <%= error %>
                </div>

            <% } 
                if(success && success.length>0) { %>
                    <div class="msg msg-info z-depth-3">
                    <%= success %>
                </div>
            <% } %>
        </div>